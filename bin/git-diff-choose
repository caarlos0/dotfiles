#!/bin/bash
# Stolen from Ayman

set -euo pipefail

FILES=$(git diff --name-only)

choose() {
  FILE=$(echo "$FILES" | fzf --prompt " " --preview 'git diff --color=always -- {}') || exit 0
  git diff -- "$FILE"
}

while true; do
  choose
done
