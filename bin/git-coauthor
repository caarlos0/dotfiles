#!/bin/sh
set -e

name=$(gh api "users/$1" | jq -r .name)
email=$(gh api "users/$1" | jq -r .email)

if [ "$email" = "null" ]; then
  email="$1@users.noreply.github.com"
fi

echo "Co-authored-by: $name <$email>"
